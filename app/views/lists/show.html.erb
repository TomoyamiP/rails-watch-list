<h1><%= @list.name %></h1>
<h2>Movies in this list:</h2>
<% if @bookmarks.any? %>
  <ul>
    <% @bookmarks.each do |bookmark| %>
      <li>
        <strong><%= bookmark.movie.title %></strong> — Rating: <%= bookmark.movie.rating %>
        <p><%= bookmark.comment %></p>
      </li>
    <% end %>
  </ul>
<% else %>
  <p>No movies in this list yet...</p>
  <%= link_to "Add your first movie", new_list_bookmark_path(@list), class: "btn btn-primary" %>
<% end %>
<%= link_to "Back to all lists", lists_path, class: "btn btn-secondary mt-3" %>
